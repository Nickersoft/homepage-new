---
import Dithering from "@/components/dithering.svelte";
import Head, { type Props as HeadProps } from "@/components/head";

export interface Props extends HeadProps {}
---

<!doctype html>
<html lang="en" transition:animate="none" class="relative size-full">
  <head>
    <Head {...Astro.props} />
  </head>
  <body class="size-full">
    <slot />

    <div class="fixed inset-0 -z-10" transition:persist="background">
      <!-- <Dithering
        class="size-full"
        client:load
        shape="warp"
        scale={1}
        speed={0.6}
        type="4x4"
        colorFront="#f5f5f5"
        colorBack="#fcf9fa"
      /> -->
    </div>
  </body>
</html>

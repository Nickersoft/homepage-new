---
import { getCollection } from "astro:content";

import FormattedDate from "@components/FormattedDate.astro";
import { getPostSlug } from "@utils";

const posts = (await getCollection("blog")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---

<div>
  <h2>Thoughts</h2>
  <ul>
    {
      posts.filter(post => post.slug.startsWith('thoughts')).map((post) => (
        <li>
          <a href={`/blog/${getPostSlug(post)}/`}>
            <FormattedDate date={post.data.pubDate} />
            {post.data.title}
          </a>
        </li>
      ))
    }
  </ul>
</div>

<div> 
  <h2>Learnings</h2>
  <ul>
    {
      posts.filter(post => post.slug.startsWith('learnings')).map((post) => (
        <li>
          <a href={`/blog/${getPostSlug(post)}/`}>
            <FormattedDate date={post.data.pubDate} />
            {post.data.title}
          </a>
        </li>
      ))
    }
  </ul>
</div>

<style lang="postcss"> 
  a {
    @apply block; 
    @apply text-lg font-sans font-normal mb-0 mt-1 py-4 rounded-lg px-4 flex flex-col gap-1;
    @apply transition-all duration-75;
    @apply -mx-4;

    &:hover {
      @apply dark:bg-white dark:bg-opacity-[0.03] bg-black bg-opacity-[0.03];
    }

    &:active {
      @apply opacity-50;
    }
  }     
</style>
